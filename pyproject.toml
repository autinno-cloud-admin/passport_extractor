[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "passport_extractor"
version = "0.0.1"
description = "Extract passport details from passport MRZ code"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools]
packages = ["passport_extractor"]
